import { createBrowserClient } from "@supabase/ssr";

// TODO: Create and return a Supabase browser client using NEXT_PUBLIC_SUPABASE_URL
// and NEXT_PUBLIC_SUPABASE_ANON_KEY. Safe to call in React components and hooks.
// Never import SUPABASE_SERVICE_ROLE_KEY here — this file is bundled into the browser.
export function createClient() {
  // TODO: return createBrowserClient(
  //   process.env.NEXT_PUBLIC_SUPABASE_URL!,
  //   process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
  // )
}
