"use client";

// Password reset request page (/reset-password).
// Shows email input; on submit calls POST /api/auth/password-reset/request.
// Always shows "If that email is registered, you'll receive a link shortly."
// (Never distinguishes registered vs unregistered email.)
export default function ResetPasswordPage() {
  // TODO: implement form state
  // TODO: call POST /api/auth/password-reset/request on submit
  // TODO: on any response (success or error), show the generic confirmation message
  return (
    <div>
      {/* TODO: <PasswordResetRequestForm /> */}
      <p>TODO: Password reset request form</p>
    </div>
  );
}
